# 상태 조건부 200초 전체전

## 상태

`SKIPPED_NO_CANDIDATE`.

PROMOTE_CANDIDATE, scale/장시간 후보 freeze, held-out 통과, 정상 proxy 통과 조건을 하나도 충족하지 못했다. 따라서 200초 episode에서 우연한 좋은 결과를 탐색하지 않았다. phase별 Damage/Cone, win/loss/tie, latency, fallback 값은 N/A다.
